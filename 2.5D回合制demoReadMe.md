

# 1.项目结构

## 1.1

## 1.2脚本

# 2.脚本

2.1技能脚本

2.2UI相关

2.3BattleStateEffectManager.cs

## 2.4BattleSystem.cs

**脚本作用**：`BattleSystem.cs` 是战斗场景的核心控制脚本，负责管理战斗流程、回合顺序、技能释放、目标选择、伤害结算以及战斗胜负判定。

**挂载位置**：该脚本挂载在 **BattleScene 中的 `BattleSystem` GameObject** 上。

**主要职责**：

- 生成玩家与敌人战斗实体
- 控制玩家选择技能与目标
- 按行动顺序执行回合
- 处理技能动画、移动和伤害结算
- 更新UI与战斗资源（战技点）
- 判断战斗胜负并切换场景

### 2.4.1BattleSystem类

**类作用**：`BattleSystem` 负责控制整场战斗的流程，包括：

- 战斗初始化
- 玩家指令选择
- 回合执行
- 技能结算
- 战斗结束

------

### 2.4.1.1字段说明

首先是在Unity中有接口的字段：

##### （1）Battle State

| 字段                | 作用                                               |
| ------------------- | -------------------------------------------------- |
| `BattleState state` | 当前战斗状态（开始、选择、执行、胜利、失败、逃跑） |

初始为Start。

------

##### （2）Spawn Points

| 字段               | 作用             |
| ------------------ | ---------------- |
| `partySpawnPoints` | 玩家单位生成位置 |
| `enemySpawnPoints` | 敌人单位生成位置 |

分别拖入`Party_Spawn_Points`,`Enemy_Spawn_Points`两个GameObject。

------

## Battlers

| 字段             | 作用             |
| ---------------- | ---------------- |
| `allBattlers`    | 战斗中的所有单位 |
| `enemyBattlers`  | 敌方单位列表     |
| `playerBattlers` | 玩家单位列表     |

------

## Battle Resources

| 字段             | 作用       |
| ---------------- | ---------- |
| `skillPoints`    | 当前战技点 |
| `maxSkillPoints` | 战技点上限 |

------

## UI

| 字段                 | 作用             |
| -------------------- | ---------------- |
| `selectionButtons`   | 目标选择按钮     |
| `battleMenu`         | 战斗操作菜单     |
| `selectionMenu`      | 目标选择界面     |
| `actionText`         | 当前行动角色提示 |
| `bottomTextPopUp`    | 底部信息提示UI   |
| `bottomText`         | 提示文字         |
| `floatingTextPrefab` | 伤害/治疗飘字    |

------

## Skill Point UI

| 字段             | 作用           |
| ---------------- | -------------- |
| `skillPointUI`   | 战技点UI容器   |
| `skillPointText` | 战技点显示文本 |

------

## 运行时变量

| 字段            | 作用               |
| --------------- | ------------------ |
| `partyManager`  | 获取当前玩家队伍   |
| `enemyManager`  | 获取当前敌人       |
| `currentPlayer` | 当前行动的玩家索引 |

------

# BattleSystem 函数说明

## 初始化

### `Start()`

初始化战斗，生成单位并启动战斗菜单。

### `ShowBattleMenuAfterDelay()`

战斗开始时显示提示文字并延迟开启操作菜单。

------

## 战斗流程

### `BattleRoutine()`

执行一整轮战斗流程并处理胜负判定。

### `AttackRoutine(int i)`

根据单位选择的行动执行攻击或技能。

### `AttackAction(attacker, targets)`

执行攻击动画、移动并结算技能效果。

### `ResolveSkillEffect(attacker, targets)`

根据技能类型对目标造成伤害或治疗。

### `RemoveDeadBattlers()`

移除血量小于等于0的单位。

------

## 单位创建

### `CreatePartyEntities()`

根据队伍数据生成玩家战斗实体。

### `CreateEnemyEntities()`

根据敌人数据生成敌方战斗实体。

------

## 玩家操作

### `ShowBattleMenu()`

显示当前玩家的操作菜单。

### `OnNormalAttackButton()`

玩家点击普通攻击按钮时调用。

### `OnBattleSkillButton()`

玩家点击战技按钮时调用。

### `ShowObjectSelectionMenu()`

根据技能类型显示目标选择界面。

### `SelectObject(int index)`

玩家选择目标后设置技能目标。

### `SelectRunAction()`

玩家选择逃跑行动。

------

## 目标选择

### `SetEnemySelectionButtons()`

设置敌方目标选择按钮。

### `SetAllySelectionButtons()`

设置友方目标选择按钮。

### `GetTargets(attacker)`

根据技能类型获取目标列表。

### `GetRandomEnemy()`

随机选择一个存活的敌人。

### `GetRandomPartyMember()`

随机选择一个存活的队友。

------

## 战斗表现

### `CalculateAttackPosition(attacker, targets)`

计算攻击者移动到的攻击位置。

### `MoveToPosition(mover, targetPos, duration)`

控制单位移动到指定位置。

### `ShowFloatingText(target, power, isHeal)`

生成伤害或治疗飘字。

------

## 战斗数据

### `SaveHealth()`

将玩家当前血量保存回队伍数据。

### `DetermineBattleOrder()`

根据行动值排序战斗单位。

------

## 战技点系统

### `ConsumeSkillPoint(amount)`

消耗指定数量战技点。

### `AddSkillPoint(amount)`

恢复战技点。

### `UpdateSkillPointUI()`

更新战技点UI显示。

### `HandleSkillPointCost(skill)`

处理技能消耗或恢复战技点。

------

# 类二：BattleEntities

## 类作用

`BattleEntities` 表示战斗中的一个单位实体（玩家或敌人），包含角色属性、技能、目标以及战斗表现组件。

------

# 字段说明

| 字段             | 作用             |
| ---------------- | ---------------- |
| `Name`           | 单位名称         |
| `CurrHealth`     | 当前生命值       |
| `MaxHealth`      | 最大生命值       |
| `Initiative`     | 行动速度         |
| `Strength`       | 攻击力           |
| `Level`          | 等级             |
| `IsPlayer`       | 是否为玩家单位   |
| `AttackDuration` | 攻击动画持续时间 |
| `NormalAttack`   | 普通攻击技能     |
| `BattleSkill`    | 战技技能         |
| `SkillData`      | 当前选择的技能   |
| `Target`         | 当前技能的目标   |
| `BattleVisual`   | 战斗表现组件     |

------

# BattleEntities 函数说明

### `SetPlayerEntityValues(...)`

初始化玩家单位数据。

### `SetEnemyEntityValues(...)`

初始化敌方单位数据。

### `SetTarget(target)`

设置技能目标。

### `UpdateUI()`

更新角色血量UI。

### `TakeDamage(damage)`

减少生命值并播放受击动画。

### `Heal(power)`

恢复生命值并更新UI。